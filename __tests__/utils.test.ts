describe('Utilities', () => {
  describe('categorize', () => {
    describe('given that rush.json exist', () => {
      it('should return valid rush.json file', () => {
        expect(2).toEqual(2);
      });
    });
  });
});
